pipeline {
    agent {
        label 'docker'
        image 'node'
    }
    stages { 
        stage('Build docker image') {
            steps {  
                sh 'node -v'
            }

        }
}

}
